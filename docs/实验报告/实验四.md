# 实验四：Spark预测分析

## 实验目的

- 熟悉Spark的基本使用方法
- 掌握Spark SQL和DataFrame的操作
- 了解大数据预测分析的基本流程

## 实验步骤

### 1. 准备工作

1. **检查Spark版本**：`spark-submit --version`
2. **准备数据**：确保HDFS上有`/user/hive/warehouse/dbtaobao/dataset/user_log/small_user_log.csv`文件

### 2. 编写Spark应用

1. **创建rebuy_analysis.py**：
   - 从HDFS读取CSV文件
   - 数据预处理和特征提取
   - 预测回头客
   - 保存结果到MySQL

2. **主要代码**：
   ```python
   from pyspark.sql import SparkSession
   from pyspark.sql.functions import col, when, count, sum
   
   # 创建SparkSession
   spark = SparkSession.builder \
       .appName("Rebuy Analysis") \
       .master("yarn") \
       .config("spark.driver.memory", "2g") \
       .config("spark.executor.memory", "4g") \
       .config("spark.executor.cores", "2") \
       .getOrCreate()
   
   # 从HDFS读取CSV文件
   df = spark.read.csv("hdfs://hbase01-train:9000/user/hive/warehouse/dbtaobao/dataset/user_log/small_user_log.csv", \
                      header=False, \
                      schema="user_id STRING, item_id STRING, cat_id STRING, merchant_id STRING, brand_id STRING, month INT, day INT, action INT, age_range INT, gender INT")
   
   # 数据预处理
   df = df.withColumn("buy_num", when(col("action") == 3, 1).otherwise(0))
   df = df.withColumn("buy_amount", when(col("action") == 3, 100).otherwise(0))
   
   # 计算用户购买次数
   user_buy_count = df.groupBy("user_id").agg(count(when(col("action") == 3, 1)).alias("buy_count"))
   
   # 预测回头客
   df = df.join(user_buy_count, on="user_id", how="left")
   df = df.withColumn("rebuy_prediction", when(col("buy_count") > 1, 1).otherwise(0))
   df = df.withColumn("prediction_score", col("buy_count") / 10.0)
   
   # 保存结果到MySQL
   result_df = df.select(
       "user_id", "item_id", "cat_id", "merchant_id", "brand_id", 
       "month", "day", "buy_num", "buy_amount", 
       "rebuy_prediction", "prediction_score"
   )
   
   result_df.write \
       .format("jdbc") \
       .option("url", "jdbc:mysql://localhost:3306/dbtaobao?useSSL=false&allowPublicKeyRetrieval=true") \
       .option("dbtable", "rebuy") \
       .option("user", "root") \
       .option("password", "123456") \
       .mode("overwrite") \
       .save()
   
   # 验证结果
   result_df.groupBy("rebuy_prediction").count().show()
   result_df.select("prediction_score").describe().show()
   
   # 关闭SparkSession
   spark.stop()
   ```

### 3. 执行Spark应用

1. **执行命令**：
   ```bash
   spark-submit --master yarn --deploy-mode client --driver-memory 2g --executor-memory 4g --executor-cores 2 /export/spark/apps/rebuy_analysis.py
   ```

2. **检查执行日志**：确保应用正常执行，无错误

### 4. 验证结果

1. **验证MySQL表**：`mysql -u root -p123456 -e "show tables in dbtaobao;"`
2. **验证数据行数**：`mysql -u root -p123456 -e "select count(*) from dbtaobao.rebuy;"`
3. **验证数据质量**：`mysql -u root -p123456 -e "select * from dbtaobao.rebuy limit 10;"`

## 实验结果

- **MySQL rebuy表**：创建成功
- **数据行数**：100000
- **预测结果**：存在有效预测结果（0或1）
- **预测分数**：存在有效预测分数

## 实验结论

实验四Spark预测分析完成，成功使用Spark进行了回头客预测分析，并将结果保存到MySQL中，验证了Spark在大数据预测分析中的应用能力。